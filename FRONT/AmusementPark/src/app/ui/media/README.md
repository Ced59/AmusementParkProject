# UI media

Reusable public media components.

Components in this folder are presentation-only: they own visual state such as carousel navigation or fullscreen display, but they do not fetch data, know domain DTOs, or perform business mapping.

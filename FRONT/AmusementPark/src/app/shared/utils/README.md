# shared/utils

Utilitaires transverses réutilisables entre plusieurs domaines.

Les helpers de mapping génériques posés en P03 vivent ici pour éviter de disperser des petites fonctions
`map`, `null -> []` ou `API -> UI` dans chaque future feature.
